'use strict';

// Liang's TODO: 
// This should be an ES6 class requires to be compiled by bebal
// It should also be a subclass of markets.js (compiled to Exchanges.js)

class PriceFeeds extends Exchanges {
	// mocked constructor arguments, please update as needed
	constructor(networkID, registryAddress) {
		super(networkID, registryAddress);
	}

	// More to be done ...
}

module.exports = PriceFeeds;
